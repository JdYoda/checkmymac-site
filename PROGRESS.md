# PROGRESS — checkmymac-site

Статический сайт CheckMyMac Pro (GitHub Pages, домен checkmymac.yoda-service.com через CNAME).
Лендинг EN + RU + privacy/terms/refunds + appcast.xml для автообновлений приложения.
С 2026-08-04 папка живёт ВНУТРИ проекта приложения: `~/Projects/checkmymac/site/`
(git-репозиторий остался отдельным — JdYoda/checkmymac-site; сливать с репо программы нельзя).

## 2026-08-04 — Доводка до продающего сайта + перенос в проект приложения

Сделано:
- Папка перенесена в `checkmymac/site/`; `make-appcast.sh` знает новый путь,
  `site/` в .gitignore репо программы.
- Hero: цифра 29 → 30+ (тик до 30 + серый «+»), слоган «Every hidden defect has
  nowhere to hide», кнопка «Download free for macOS» — прямая ссылка на DMG
  через `releases/latest/download/`.
- Реальные скриншоты приложения: витрина «Inside the app» из 6 кадров
  (PDF-паспорт, клавиатура, трекпад, стресс-график, SSD, Touch ID);
  WebP + PNG-fallback в `img/web/`; исходники в `img/raw|shots` (НЕ в git —
  на сырых кадрах серийник Юриного Air).
- Download-секция с таблицей Free (19 проверок) vs Full + подпись версии:
  `make-appcast.sh` при каждом релизе сам обновляет имя DMG и span.dl-ver
  на обеих языковых страницах.
- Актуализация метрик: 31 пункт чек-листа, 72 модели, +строки SSD и AUDIO в spec.
- FAQ расширен до 9 вопросов (Free, безопасность скачивания, обновления, языки).
- Русская версия `/ru/` — живой русский текст (не перевод-калька), переключатель
  EN/RU в шапке, hreflang на обеих страницах и в sitemap.
- Техника: favicon-32 + apple-touch-icon (из иконки приложения), og.png 1200×630
  (исходник `img/og-src.html`), robots.txt, sitemap.xml, 404.html.
- Юр. страницы: даты 2026-08-04; refunds переработан (сначала «попробуй Free»,
  14 дней с рассказом «что не так», деактивация лицензии, секция Abuse —
  «no questions asked» удалено); terms + секция Free version (официальный
  источник — сайт и GitHub Releases); privacy — обновления с GitHub.
- РАЗАССОЦИАЦИЯ с Yoda Service (память no-yoda-branding-public): на сайте ни
  одного упоминания имени и ссылок на yoda-service.com; формула — «built inside
  a large Apple repair service, published by an independent developer»;
  контакт везде yukkaperehenen@gmail.com.
- Проверено: ширины 320/375/768/1280 (EN и RU, без горизонтального скролла),
  все ссылки и картинки, HTML-парсинг всех 6 страниц, ссылка DMG отвечает 200,
  якорь #pricing на месте (на него ссылается кнопка «Купить» в приложении),
  appcast.xml не тронут.

Дальше:
- git push (публикация = деплой) — только с явного разрешения Юры.
- Когда откроются продажи: раскомментировать кнопки Lemon Squeezy в обеих
  версиях (заготовки в HTML, плейсхолдеры LS_CHECKOUT_*), убрать .soon.
- Публичная сборка приложения без брендинга Yoda Service — уйдёт со следующим
  релизом (правки уже в dev).
- Возможная будущая задача: свой домен вместо checkmymac.yoda-service.com
  (при миграции не сломать фид обновлений установленных копий).

## 2026-07-27 — Редизайн по скиллу Hallmark

Сделано:
- Полный редизайн лендинга: макроструктура Stat-Led (hero вокруг цифры «29»),
  бланк отчёта вместо фейкового окна macOS, таблица-протокол вместо карточек,
  минимальная навигация N9, футер-колофон.
- Дизайн-система зафиксирована: `design.md` (правила) + `tokens.css` (OKLCH-палитра
  «уголь + янтарь», шрифты Space Grotesk + IBM Plex Mono, 4pt-отступы).
- Юр. страницы переведены на общие токены через `legal.css`.
- Память скилла: `.hallmark/log.json`.
- Проверено: слоп-тест Hallmark, ширины 320/375/1280, контрасты, reduced-motion.
- Коммит 937d2a9, запушен в июле вместе с appcast-коммитами.

Важные решения:
- Палитра бренда (уголь + янтарь) сохранена — совпадает с приложением.
